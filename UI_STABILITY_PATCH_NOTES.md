# XMusic UI + Stability Patch

- Keeps the startup-safe player initialization from the previous startup fix.
- Keeps the dynamic Piped resolver and fallback instances.
- Replaces raw resolver/hostname errors with user-friendly error cards.
- Refreshes the Compose UI: Home, Library, Audio, Analyzer, Settings.
- Adds a compact mini-player above bottom navigation.
- Adds a custom XMusic launcher icon instead of the default Android robot.
- Keeps Media3 playback and existing DSP components intact.
